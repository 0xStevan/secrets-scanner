################################################################################
# WARNING: ALL "SECRETS" IN THIS FILE ARE FAKE.
#
# These strings are formatted to MATCH the regex patterns the scanner detects,
# but the credentials themselves are junk. Do NOT replace them with real keys
# for "better testing" — if you commit a real secret here, it is leaked forever
# (Git history is permanent; rotating the key is your only recourse).
#
# This file exists so the scanner has something to find when we test it.
################################################################################

# Fake AWS Access Key ID
aws_access_key = "AKIAIOSFODNN7EXAMPLE"

# Fake AWS Secret Access Key — using the AWS-standard env var name
aws_secret_access_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

# Fake GitHub Personal Access Token
github_token = "ghp_1234567890abcdefghijklmnopqrstuvwxyz"

# Fake generic API key
api_key = "sk_test_FAKE1234567890abcdefghijklmnop"

# Fake private key (header only — real keys are multi-line)
private_key = "-----BEGIN RSA PRIVATE KEY-----"

# Fake Slack token
slack_token = "xoxb-FAKE1234567890-FAKE1234567890-abcdefghijklmnopqrstuvwx"

# A line with no secrets — scanner should NOT flag this
normal_line = "this is just a regular string with no credentials"
